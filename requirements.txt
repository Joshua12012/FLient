# ── Core FL framework ─────────────────────────────────────────────────────────
flwr==1.7.0

# ── ML framework ──────────────────────────────────────────────────────────────
# On PC: install normally
# On Termux (Android): torch CPU-only wheel auto-selected
torch>=2.0.0
torchvision>=0.15.0

# ── Data / numerics ───────────────────────────────────────────────────────────
numpy>=1.24.0

# ── System profiling (adaptive_serving.py) ───────────────────────────────────
# On Termux: psutil installs fine
psutil>=5.9.0

# ── Visualisation (comm_analysis.py) — optional on phones ─────────────────────
# Comment this out on Termux if install fails (comm_analysis still prints text)
matplotlib>=3.7.0

# ── Mobile GUI / optional frontend
kivy>=2.0.0

# ── Optional TensorFlow/TFLite support for mobile runtime
# For desktop (Windows/Linux/Mac):
# pip install tensorflow
# For mobile runtime (Android/Raspberry Pi):
# pip install tflite-runtime
